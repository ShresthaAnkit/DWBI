# Keep track of every query
import logging

import datetime
from os import path

class Logger:
    def log_initialize():
        pass

    def log_message(msg):
        msg = msg
        log_file.write(str)